/**
 * @file EcosystemSettings.cpp
 * @author Juan Luque Roldán
 * @brief Unidad de traducción de UEcosystemSettings, deliberadamente vacía.
 *
 * La clase no tiene lógica de runtime: los valores por defecto se declaran en la
 * cabecera y el .ini los sobrescribe al cargar el módulo, así que aquí no queda nada
 * que definir. El fichero existe para dar una unidad de compilación a la clase y para
 * que el generador de código de reflexión emita su módulo a partir de la cabecera.
 *
 * @ingroup eco_config
 */

#include "Config/EcosystemSettings.h"
